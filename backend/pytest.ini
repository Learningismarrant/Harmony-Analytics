[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    engine: tests de fonctions pures (no DB, no async)
    service: tests de la couche service (mock repo)
    router: tests HTTP end-to-end (mock service)
